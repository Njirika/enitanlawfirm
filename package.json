{
  "name": "workspace",
  "version": "1.0.0",
  "description": "Premium legal services platform for Enitan Afolabi & Company.",
  "license": "MIT",
  "engines": {
    "node": ">=20.6.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "concurrently \"pnpm run --filter @workspace/api dev\" \"pnpm run --filter @workspace/web dev\"",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "lint": "pnpm -r run lint",
    "start": "pnpm run --filter @workspace/api start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "concurrently": "^9.1.2"
  }
}
